const Primeiro = () => {
  const welcomeMessage = "Seja Bem vindo!";

  return (
    <div>
      <h2>Primeiro Componente</h2>
      <p>{welcomeMessage}</p>
    </div>
  );
};

export default Primeiro;
